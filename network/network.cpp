#include "network.h"

Network::Network(int inputLayerRows, int inputLayerCols, std::vector<int> hiddenLayerSizes, int outputLayerSize, std::vector<std::string> activationFuncs, bool convolutional)
{
}
